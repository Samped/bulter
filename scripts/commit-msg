#!/usr/bin/env bash
# Reject Cursor co-author trailers (install: ln -sf ../../scripts/commit-msg .git/hooks/commit-msg)
msg_file="${1:?}"
if grep -qiE '^(Co-authored-by|Made-with):.*cursor' "$msg_file"; then
  echo "commit-msg: remove Cursor attribution trailers before committing." >&2
  exit 1
fi
