#!/usr/bin/env bash
# Strip Cursor co-author trailers (install: ln -sf ../../scripts/commit-msg .git/hooks/commit-msg)
msg_file="${1:?}"
tmp="$(mktemp)"
grep -viE '^(Co-authored-by|Made-with):.*cursor' "$msg_file" > "$tmp"
mv "$tmp" "$msg_file"
